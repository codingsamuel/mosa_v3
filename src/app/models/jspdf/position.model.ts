export interface IPosition {
    /**
     * -x = offset from right
     * +x = offset from left
     */
    x: number;
    /**
     * -y = offset from bottom
     * +y = offset from top
     */
    y: number;
}
